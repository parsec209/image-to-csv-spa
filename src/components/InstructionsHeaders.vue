<template>
  <div>
    <p>
      Use a saved <strong>header</strong> to auto-populate the header row values in a recurring docs table. Headers can be added, edited, and deleted on
      the <b-link to="/index/header">My headers</b-link> page. Saved headers are convenient when more than one recurring document uses the same header fields. 
      Keep in mind that selecting a saved header will clear all of the table's current values, including the table body (a warning will pop-up).
    </p>
  </div>
</template>    


<script>
export default {
  name: 'InstructionsHeaders'
}
</script>
